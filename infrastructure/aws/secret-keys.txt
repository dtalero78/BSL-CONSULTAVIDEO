# Claves de entorno que se guardan como secrets en SSM Parameter Store (SecureString).
# Fuente única de verdad: la usa scripts/load-secrets-to-ssm.sh (para subir los valores
# desde backend/.env) y secrets.tf (para referenciarlos en el task def de ECS).
#
# NO incluir aquí NODE_ENV, PORT, ALLOWED_ORIGINS, APP_URL ni BASE_URL:
# esas son configuración (no secreto) y se definen directo en el task def (ecs.tf).
#
# Si agregas una variable secreta nueva a backend/.env, agrégala también aquí.
# Derivado de backend/.env (26 claves reales; se excluyen las 3 de config).

JWT_SECRET
OPENAI_API_KEY
POSTGRES_DATABASE
POSTGRES_HOST
POSTGRES_PASSWORD
POSTGRES_PORT
POSTGRES_USER
SMTP_FROM
SMTP_HOST
SMTP_PASS
SMTP_PORT
SMTP_SECURE
SMTP_USER
TWILIO_ACCOUNT_SID
TWILIO_API_KEY_SECRET
TWILIO_API_KEY_SID
TWILIO_AUTH_TOKEN
TWILIO_TEMPLATE_VIDEOCONSULTA_SUELTA
TWILIO_VOICE_ACCOUNT_SID
TWILIO_VOICE_AUTH_TOKEN
TWILIO_WHATSAPP_FROM
TWILIO_WHATSAPP_TEMPLATE_SID
WHAPI_TOKEN
BSL_PLATAFORMA_PASS
